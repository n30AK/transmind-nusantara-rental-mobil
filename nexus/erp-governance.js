(()=>{'use strict';const boot=()=>{if(!window.NXSB){setTimeout(boot,300);return}if(document.getElementById('nx-erp-governance'))return;const sb=window.NXSB;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fmt=n=>new Intl.NumberFormat('id-ID',{maximumFractionDigits:0}).format(Number(n||0));function nav(){const sec=[...document.querySelectorAll('.nav-section')].find(x=>/Management/i.test(x.textContent));if(!sec)return;const sm=sec.querySelector('.submenu');if(!sm||[...sm.querySelectorAll('[data-page]')].some(x=>x.dataset.page==='erp-governance'))return;const b=document.createElement('button');b.type='button';b.dataset.page='erp-governance';b.textContent='ERP Governance';b.onclick=render;sm.appendChild(b)}function page(){const p=document.createElement('section');p.id='nx-erp-governance';p.className='page';p.innerHTML='<div class="hero"><div><div class="eyebrow">ERP CORE</div><div class="title">Governance & Control</div><div class="desc">Approval, audit trail, organisasi dan kontrol keputusan perusahaan.</div></div><div class="hero-actions"><button class="secondary" id="erpRefresh">↻ Refresh</button></div></div><div id="erpBody"><div class="notice">Memuat governance…</div></div>';document.getElementById('pages').appendChild(p);p.querySelector('#erpRefresh').onclick=load}async function load(){const el=document.getElementById('erpBody');const {data,error}=await sb.rpc('nexus_erp_governance_center');if(error){el.innerHTML='<div class="notice bad">'+esc(error.message)+'</div>';return}const x=data||{};el.innerHTML='<div class="grid" style="grid-template-columns:repeat(5,1fr);gap:12px"><div class="card"><small>PENDING APPROVAL</small><h2>'+fmt(x.pending_approvals)+'</h2></div><div class="card"><small>APPROVED</small><h2>'+fmt(x.approved_approvals)+'</h2></div><div class="card"><small>REJECTED</small><h2>'+fmt(x.rejected_approvals)+'</h2></div><div class="card"><small>AUDIT EVENTS</small><h2>'+fmt(x.audit_events)+'</h2></div><div class="card"><small>ORG UNITS</small><h2>'+fmt(x.org_units)+'</h2></div></div><div class="card" style="margin-top:14px"><h3>ERP Control Layer</h3><p>Approval engine dan audit trail sudah aktif di backend. Data operasional tidak diubah otomatis oleh modul ini.</p></div>'}function render(){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));page();const p=document.getElementById('nx-erp-governance');p.classList.add('active');document.querySelectorAll('.submenu button').forEach(b=>b.classList.toggle('active',b.dataset.page==='erp-governance'));const bc=document.getElementById('breadcrumb');if(bc)bc.textContent='Management / ERP Governance';load()}window.NXERPGovernance={render,load};nav()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot()})();
